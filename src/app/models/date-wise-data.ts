export interface DateWiseData {
  country?: string;
  cases?: number;
  date?: Date;
}

// Here we intend to create a key value pair which will countain a country as key and
// a list of date-wise-data object as value
